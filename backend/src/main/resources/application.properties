spring.application.name=backend

# ================== DATABASE ==================
spring.datasource.url=jdbc:mysql://localhost:3306/petguardian?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=safadao123

# Dialeto do JPA/Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Criar/atualizar tabelas automaticamente
spring.jpa.hibernate.ddl-auto=update

# Mostrar SQL no console
spring.jpa.show-sql=true

# ================== SWAGGER / OPENAPI ==================
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# ================== SERVER ==================
server.port=8080

# ================== EMAIL CONFIG ==================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=felipecostamonteiro@gmail.com
spring.mail.password=fzvo tqrs yzws ndku
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ================== JWT CONFIG ==================
# Gere uma chave segura de 256 bits em Base64 (usando Keys.secretKeyFor(SignatureAlgorithm.HS256))
jwt.secret=80Qqnaw+oHseRsueAa1QmQY1j/RfthVdIlFDvgV+Gss=
jwt.expiration=36000000

# ================== CORS ==================
# (opcional, �til para debug no Spring 3+)
spring.web.cors.allowed-origins=http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=Authorization,Content-Type
spring.web.cors.allow-credentials=true
